#include<iostream>
using namespace std;
strlen(char* i)
{
	if (*i == '\0')
	return 0;
	else 
	return 1 + strlen(i+1);
}
int main()
{
	char s[100];
	cout<<"enter the string: ";
	gets(s);
	cout<<"length of string is : "<<strlen(s);
	return 0;
}
